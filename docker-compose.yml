services:
  mongodb:
    image: mongo:8.0
    container_name: video-archive-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: video_archive
    volumes:
      - mongodb_data:/data/db

volumes: